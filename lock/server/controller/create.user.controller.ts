import handleAsync from "server/utils/async";

export const createUser = handleAsync(async (_req, _res, _) => {

    //TODO check NFT for the user

    //TODO if nft is matched, then create a new user and deploy nft
    

})